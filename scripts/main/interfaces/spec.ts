/**
 * 组件中的测试文件
 * 主要获取测试所依赖的模块
 *
 * @export
 * @interface NcSpec
 */
export interface NcSpec {
  /**
   * 名称
   */
  name?: string;
  /**
   * 导入路径
   */
  import?: string;
  /**
   * 模块名称
   */
  module?: string;
}
