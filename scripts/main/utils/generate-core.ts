import { writeFileSync } from 'fs-extra';
import { NcCore } from '../interfaces/core';

/**
 * 生成 core 中类型文件，json 格式
 *
 * @export
 * @param {string} genDir
 * @param {NcCore} core
 */
export function generateCore(genDir: string, core: NcCore) {
  writeFileSync(
    `${genDir}/docs.types.ts`,
    `/* DO NOT EDIT, file generated by ng-nest */
import { AppCore } from '@interfaces';
export const core: AppCore = ${JSON.stringify(core, null, 2)}`,
    'utf8'
  );
}
