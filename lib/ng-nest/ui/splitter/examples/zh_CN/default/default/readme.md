---
order: 1
label: '基础用法'
---

分割器的基础用法。

- 使用 `x-splitter` 创建可调整大小的分割面板布局。
- 使用 `x-splitter-panel` 定义每个面板，可以设置 size、min 和 max 约束。
- 使用 `x-splitter-bar` 在面板之间添加可拖动的分隔条。
- 通过 `direction` 属性支持水平（默认）和垂直布局。
