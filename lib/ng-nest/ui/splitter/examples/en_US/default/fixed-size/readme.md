---
order: 2
label: 'Fixed Size'
---

Splitter with fixed size units.

- Supports multiple size units including percentage (%), pixels (px), and rem.
- Set the `size` property with different units to control panel dimensions.
- Use `min` and `max` properties to constrain resizing behavior.
- Panels without explicit size will automatically fill remaining space.
