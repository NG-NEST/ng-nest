import { TemplateRef } from '@angular/core';

/**
 * BackTop 组件名
 * @selector x-back-top
 * @decorator component
 */
export const XBackTopPrefix = 'x-back-top';

/**
 * BackTop @Input
 */
export interface XBackTopInput {
}
