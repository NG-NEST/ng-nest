---
order: 13
label: 'Custom portal'
---

- Use the `portalTemp` property to set custom popup content.
- The `auto-complete` component contains `portalNodeClick` node selection and `portalClose` closing pop-up events.
