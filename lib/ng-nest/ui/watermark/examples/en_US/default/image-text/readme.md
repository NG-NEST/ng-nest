---
order: 3
label: 'Image & Text'
---

- Use both `content` and `imageSrc` properties to achieve image-text overlay effect.
- Use `imagePosition` to control the position of the image relative to text (top/center/bottom).
