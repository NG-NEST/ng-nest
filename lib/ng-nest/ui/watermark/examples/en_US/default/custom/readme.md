---
order: 4
label: 'Custom Spacing'
---

- Use `gapX` and `gapY` to control horizontal and vertical spacing between watermarks.
- Use `rotate` to set the rotation angle.
- Use `alpha` to adjust opacity.
