---
order: 13
label: 'Custom portal'
---

- Use the `portalTemp` property to set custom popup content.
- The `select` component contains `portalNodeClick` node selection and `portalClose` closing pop-up events.
